GEMINI_API_KEY=
PORT=8787
PRIMARY_MODEL=gemini-2.5-pro
VERIFIER_MODEL=gemini-2.5-flash
MAX_VERIFIER_CONCURRENCY=6

# Optional: when set, /api/chat requires header X-Lumen-Token matching this value.
# Used in deployed builds to gate the public URL.
LUMEN_ACCESS_TOKEN=
